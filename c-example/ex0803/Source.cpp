#include <stdio.h>
int main() {
	int a, b;
	a = 6;
	b = a - 2;
	if (a > b)
		printf("%d is greather than %d\n", a, b);
	return(0);
}

//If'e ait olan tek ifadenin girintilenmesi gelenekseldir, ancak bu zorunlu deðildir. Bazen, özellikle de ifade kýsa olduðunda, her ikisi de ayný satýrda birleþtirilir: