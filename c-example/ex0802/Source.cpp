#include <stdio.h>
int main() {
	int a, b;
	a = 6;
	b = a + 2;
	if (a > b) {
		printf("%d is greater than %d\n", a, b);
	}
	return(0);
}

// if koþulu yanlýþ olduðundan süslü parantez içindeki ifade yürütülmez.